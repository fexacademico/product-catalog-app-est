// Utilice este diseño para las tarjetas del catalogo

/* <div class="card">
  <div>
    <h3>${item.name}</h3>
    <img src="${item.imageUrl}" alt="${item.name}">
    <p>${item.description}</p>
  </div>
  <div class="card-footer">
    <button onclick="deleteProduct(${item.id})">Eliminar</button>
  </div>
</div> */
